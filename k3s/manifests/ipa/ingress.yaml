apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ipa
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - ipa.arpatek.dev
      secretName: arpatek-dev-tls
  rules:
    - host: ipa.arpatek.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ipa-external
                port:
                  number: 443
